"""Compatibility aliases for the former ``renglo.docs`` package.

``renglo.docs`` was renamed to ``renglo.files``. These modules re-export
the files implementations under the historical names so existing extension
imports keep working.
"""
